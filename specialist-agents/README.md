# $(basename "$d" | tr '-' ' ' | awk '{for(i=1;i<=NF;i++){ $i=toupper(substr($i,1,1)) substr($i,2)}; print}')

Placeholder README. This folder will be populated as you progress through the roadmap.

Start here: `docs/00-roadmap.md`
